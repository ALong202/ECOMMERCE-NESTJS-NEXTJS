version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: latest
    commands:
      - ls -la
      # - cd shoppy-backend
      - npm install -g pnpm
      - pnpm install
  build:
    commands:
      - ls -la
      # - cd shoppy-backend
      - pnpm prisma generate
      - pnpm build
artifacts:
  # base-directory: shoppy-backend
  files:
    - "**/*"
